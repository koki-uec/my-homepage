---
const { label, value, note } = Astro.props;
---

<div class="statcard">
    <h3>{label}</h3>
    <p>{value} {note && <small>{note}</small>}</p>
</div>

<style>
    .statcard { border: 1px solid #ddd; padding: 1rem; }
</style>